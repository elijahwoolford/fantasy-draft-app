# Legacy Streamlit prototype

Original fantasy draft lottery UI (Streamlit).

## Run

From this directory:

```bash
pip install -r requirements.txt
streamlit run app.py
```

Or the variant with the enhanced countdown:

```bash
streamlit run app_2.0.py
```

`./sleeper.jpeg` must stay next to these scripts (it is included here).
